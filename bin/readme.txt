1、我使用的是32位的JRE，若要运行wc.exe，请将bin中的jre.1.8.0_161文件一同下载

2、wc.bat为运行脚本，包含博客内提到的十个测试用例，可在控制台直接运行，若要使用脚本运行，请下载整个bin文件夹

3、指令中的文件路径可以使用相对路径

4、使用-o指令时，请指明输出文件位置

5、使用-s指令时，-s直接接文件夹的名字，不加像*.c一样的后缀，会处理文件夹下所有的文件，例如：wc.exe -s -c input -o output.txt

6、-s -e -o三个指令的位置只能在第一位，倒数第四位和倒数第二位（有-o）或第一位，倒数第二位（无-o）